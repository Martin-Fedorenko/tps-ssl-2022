Ejecute en su consola los siguientes comandos para compilar Flex y Bison:

```flex lexico.l```

```bison -yd sintactico.y```

```gcc y.tab.c lex.yy.c```

Ejecute en su consola el siguiente comando para ejecutar el programa:

```a.exe```
